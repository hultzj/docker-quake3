apiVersion: v1
kind: Service
metadata:
  labels:
    app: quake3
  name: aquak3
spec:
  type: ClusterIP
  ports:
    - protocol: TCP
      port: 9000
      targetPort: 9000
  selector:
    app: quake3
