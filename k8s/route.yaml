apiVersion: route.openshift.io/v1
kind: Route
metadata:
  labels:
    app: quake3
  name: quake3
spec:
  port:
    targetPort: 9000
  to:
    kind: Service
    name: quake3
    weight: 100
  wildcardPolicy: None
